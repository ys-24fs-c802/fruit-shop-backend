<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.cofile.fruitshop.backend.mapper.PaymentMapper">
    <insert id="insertPayment">
        INSERT INTO payments (payment_id, payer_id, amount, state)
        VALUES (#{paymentId}, #{payerId}, #{amount}, #{state})
    </insert>
    <select id="findPaymentById" parameterType="int" resultType="kr.co.cofile.fruitshop.backend.dto.PaymentDTO">
        SELECT id, payment_id, payer_id, amount, state, created_at, updated_at
        FROM payments
        WHERE id = #{id}
    </select>
</mapper>